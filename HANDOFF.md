# V12 Coaching App — Handoff Doc

Read this before touching the codebase. It's the current, verified state of the app as of 2026-09-23 — not aspirational, not historical. Where something is unfinished, it says so explicitly.

## 1. What this is

A coaching platform for a fitness coach (V12) and their clients. The coach runs client intake, AI-assisted program generation, phase progression, check-ins, goals, and business metrics from a coach dashboard. Clients get a tiered app experience (full coaching / program-only / starter) for logging workouts, nutrition, check-ins, and viewing their program/roadmap.

Client-facing copy should match the voice in the V12 Bible (Notion) — read it before generating any client-facing text; don't just pull data from it.

## 2. Tech stack

- **Frontend**: Vite + React 18 SPA. **Not** Next.js — no file-based routing. `src/App.jsx` is a large root component that renders `CoachDashboard` or `ClientDashboard` based on role, each managing its own `page` state (hand-rolled nav, no react-router).
- **Language**: plain JS/JSX, no TypeScript.
- **Backend**: Vercel serverless functions in `/api/*.js` (Node, ESM), separate from the Vite build.
- **Database/Auth**: Supabase (Postgres + Auth + Storage). Schema lives in `db/*.sql` — one baseline `schema.sql` plus ~50 incremental `add_*`/`fix_*` files, applied **manually** via the Supabase SQL editor (no migration runner/CLI in the loop).
- **Email**: Resend (`api/_lib/resend.js`).
- **AI**: Anthropic (`api/_lib/anthropic.js`, model `claude-opus-4-8`), used for program generation, nutrition generation, check-in summaries/recaps, goal insights, and phase recommendations.
- **CRM/intake source**: Notion (`@notionhq/client`) — used as the source of truth for client intake, program templates, and CRM pipeline. Program templates are now sourced from Notion, **not** the legacy Supabase `program_templates` table.
- **Payments**: Payoneer — **stubbed, not implemented** (see §6).
- **Charts**: Recharts. **Tests**: Node's built-in `node --test`. **CI**: GitHub Actions runs `npm ci && npm test && npm run build` on PR/push to main.

## 3. Architecture / how routing works

No page-based routing — `App.jsx` picks `CoachDashboard` vs `ClientDashboard` from `profiles.role` (plus a hardcoded `COACH_EMAIL` fallback in `src/lib/constants.js`). Each dashboard keeps a `page` string in state and a `Sidebar` renders nav items for it.

**Coach pages**: `dashboard` (overview/at-risk/alerts), `clients` (per-client deep dive), `crm` (lead pipeline board), `metrics` (business/content KPIs), `assess` (pre-signup assessments), `templates` (legacy program templates), `progression` (progression-model library), `library` (shared resources).

**Client pages depend on tier**:
- **Full coaching**: dashboard, check-in (daily/weekly), workouts, progress, program, nutrition, habits, resources, schedule, roadmap.
- **Program-only**: program home, program, workouts, progress, more — no check-in flow.
- **Starter**: starter home, workouts, schedule, resources — no Progress/Plan tabs at all (deliberate spec restriction, not a bug if it looks missing).

Auth screens: `LoginScreen`, `IntakeForm` (public lead capture), `ResetPasswordScreen`.

Server-side coach gating: `api/_lib/auth.js` `requireCoach()` verifies the bearer token via Supabase and checks `profiles.role === 'coach'`. API routes use the Supabase **service-role key** and bypass RLS entirely — RLS is the client-side safety net, not the only one.

## 4. Core data model (by group)

- **Core**: `profiles` (role + client_type + V12 3-system assessment scores), `programs`, `exercises`, `nutrition_plans`, `daily_checkins`, `weekly_checkins`, `workout_logs`, `progress_photos`, `habits`/`habit_logs`, `coach_notes`, `conversations`, `resources`, `program_versions` (immutable snapshots).
- **Notion migration staging**: `staged_clients`/`staged_daily_checkins`/`staged_weekly_checkins`/`staged_exercises`/`staged_workout_logs`/`staged_nutrition_plans` — claimed into real tables via `claim_staged_data()` RPC on signup.
- **Assessment/goals**: `client_assessments` (coach's pre-signup intake), `client_goals` + `client_goal_insights`, `client_goal_scores` (write-only history, nothing reads it yet — see §6).
- **Coaching ops**: `coach_messages` (versioned), `daily_metrics` (coach-entered business KPIs, manual), `exercise_diagrams`, `meal_logs` (additive to `daily_checkins` macros, not overwriting), `client_onboarding_tasks` (day-0 gate: assessment → coach_review → roadmap_ready).
- **Phases/programming**: `program_phase_recommendations` (AI-advisory, requires explicit coach approve/modify/hold/reject — mirrors the note in `db/add_phase_recommendations.sql` you had open), `program_phase_history` (append-only log), `program_phases` (coach's forward-looking planned sequence, read by `ProgramRoadmap`), `progression_models` (coach-editable methodology library fed into AI prompts), `programs.top_phase`/`sub_phase`, `exercise_upgrades` (exists, unused — see §6).
- **Scheduling/CRM/ops**: `scheduled_workouts` (per-date override vs. legacy weekday matching), `client_summaries` (monthly AI recap text), `leads` (unified CRM/intake table), `rate_limits` (Postgres-backed limiter, survives cold starts), `starter_checkout_sessions`.

**RLS pattern**: `public.is_coach()` (SECURITY DEFINER) gates almost every policy — `client_id = auth.uid() OR is_coach()` for reads, coach-only for most writes; clients can write their own `daily_checkins`/`weekly_checkins`/`workout_logs`/`habit_logs`.

**Known gap**: `db/fix_client_goals_write_policy.sql` and `db/add_upgrade_requests.sql` are empty/stub files even though `upgrade_requests` and the goals write policy are live in prod and referenced in app code. The `db/` folder is the intended source of truth for schema but is **not fully accurate** for these two — those changes were applied directly in Supabase's SQL editor without being checked in. If you touch either area, verify actual prod schema before trusting the migration file.

## 5. API endpoints

Exactly **12 files** in `api/*.js` — this matches a hard constraint: **Vercel Hobby plan caps serverless functions at 12, and this project is at the limit.** Do not add new files under `api/` — extend an existing route by branching on payload shape instead (this is already the pattern: `goal-insight.js` multiplexes goal insights / phase recommendations / roadmap generation by which ID is present in the body; `starter-checkout.js` multiplexes checkout-start and payment-confirm).

- **AI generation**: `generate-program.js`, `goal-insight.js` (3-in-1), `summary.js` (monthly recap).
- **Leads/CRM/onboarding**: `submit-application.js`, `check-accepted.js`, `link-lead.js`, `sync-lead-to-notion.js`.
- **Notion sync**: `sync-client.js`, `notion-goal.js`, `list-templates.js`.
- **Payments**: `starter-checkout.js`.
- **Cron**: `checkin-digest.js`.
- Shared server libs (not counted against the function cap since they're imported, not deployed as routes): `api/_lib/{anthropic,auth,notion,notionCrm,notionTemplates,leads,paymentProvider,rateLimit,resend,scores,starterActivation,strengthTrends,supabaseAdmin}.js`.

`vercel.json` sets `maxDuration: 180` on the three AI-heavy routes (`generate-program`, `summary`, `goal-insight`) — these are the ones most likely to hit timeout issues under load.

## 6. What's mid-build / stubbed / deliberately incomplete

- **Payoneer payments — not implemented.** `api/_lib/paymentProvider.js`: `createCheckoutSession()` throws "Payoneer integration not implemented yet" without an API key; `parseWebhookEvent()` always returns null. Code comments reference `api/payoneer-webhook.js` and `api/admin-confirm-starter-payment.js` as routes that don't exist. Starter checkout currently falls back to a manual coach-confirm path. **If asked to "finish payments," this is the work.**
- **`exercise_upgrades` table has zero code references.** The migration comment points to an `api/advance-phase.js` "Advance to Next Phase" flow that does not exist. Likely scaffolding for a feature that got descoped — possibly related to the phase-aware generation effort that was built and fully reverted in a prior session (PRs #66-72; root cause of a 504 timeout was never resolved, and "Fast Mode" work is blocked on it — don't re-propose exercise-count caps as the fix without re-reading that history).
- **`client_goal_scores`** is explicitly write-only per its own migration comment — daily snapshots are stored but nothing computes trends/predictions from them yet.
- **Starter content library** — `add_starter_onboarding.sql` notes the "pick a workout" flow isn't trackable yet, pending a Notion DB that doesn't exist yet.
- **`program_templates` table + coach "Templates" panel are legacy.** Program generation reads templates from Notion now; the old Supabase table/UI still exist and render but aren't in the live generation path. Don't assume editing a template there affects generated programs.
- **Two empty migration stub files** noted in §4 — schema drift risk if anyone assumes `db/*.sql` is complete.

## 7. Environment variables

Server (Vercel): `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `ANTHROPIC_API_KEY`, `NOTION_API_KEY`, `NOTION_DATABASE_ID`, `NOTION_PROGRAM_LIBRARY_DB_ID`, `NOTION_CRM_DATA_SOURCE_ID`, `NOTION_CHECKINS_DB_ID`, `RESEND_API_KEY`, `COACH_NOTIFICATION_EMAIL`, `PAYONEER_API_KEY` (unused until payments are built), `CRON_SECRET`.

Frontend (Vite, build-time): `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` (note: `src/supabaseClient.js` has hardcoded dev fallbacks if these are unset — don't rely on that in prod).

One-off migration scripts only (not needed on Vercel): `SHEET_ID`, `SHEET_RANGE`, `GOOGLE_API_KEY`, `NOTION_MEASUREMENTS_DB_ID`.

## 8. Cron jobs

`vercel.json` → `checkin-digest` runs daily at 12:00 UTC, hits `api/checkin-digest.js`, requires `Authorization: Bearer <CRON_SECRET>` (Vercel sends this automatically for cron triggers, rejects any other caller). It rolls up the last 24h of daily/weekly check-ins into a single digest email to the coach via Resend, rather than per-check-in emails.

## 9. Feature inventory

**Client-facing**: daily/weekly check-ins, habit tracker, workout logging with history/charts, progress photos (grouped by weekly check-in), body composition/BMI, goal tracking with computed scores, auto-detected milestones, macro calculator, nutrition plan + per-meal logging (additive), resource/recipe library, custom workout scheduling (per-date overrides), program roadmap view, training-partner program sharing, tiered access (coaching/program-only/starter) with access-window expiry, monthly AI recap banner + email.

**Coach-facing**: AI program + nutrition generation (Notion intake/templates → Anthropic → Supabase), coach notes + conversation log, versioned coach messages, phase control + history + planned roadmap, AI phase recommendations (advisory-only, approve/modify/hold/reject), AI-generated full roadmap proposal, progression-model library, client assessments, CRM/lead pipeline board, business+content metrics dashboard, client archive, program version history (snapshot/restore), at-risk/alert panels (missed check-ins, low adherence, milestone/phase/onboarding alerts), superset/circuit workout-block grouping.

## 10. Working conventions worth knowing

- **Don't propose big architecture changes or risky refactors unprompted** — this team ships in small, bounded slices; propose small pieces first and ask before large restructuring.
- **Don't add new `api/*.js` files** — Vercel Hobby plan is at the 12-function cap; extend an existing endpoint instead.
- Silent Supabase write failures are very often an **RLS policy mismatch**, not a form/UI bug — check `is_coach()` policies first.
- A prior "phase-aware generation" build was fully reverted after real problems (504s, blocked Fast Mode) — read that history before re-attempting anything phase-generation-related.
- No error boundary exists at the app root — a single bad ref/render can white-screen the whole app (has happened once, in `CoachHome`). Be careful with refactors that touch shared render paths.

## 11. V12 Head Coach initiative (in progress, not yet live)

A separate, much larger architecture spec was introduced 2026-09-23: an AI-driven coaching decision pipeline — `check-in → event → task → context assembly → safety gate → rule engine → Claude → structured output → validation → authority check → recommendation → coach approval → intervention → outcome → audit` — being built on top of this app in small, owner-approved slices (per the working convention in §10; this is exactly the kind of change that convention exists for).

**Status: HC-001 (repo audit) and HC-002 (database foundation) are complete and applied to prod. HC-003 onward has not started.** None of this is wired into the running app yet — no UI, no endpoint, no trigger fires it. It's inert schema waiting on HC-003+.

**HC-002 — what was actually applied to Supabase:**
- 5 new tables: `head_coach_tasks`, `head_coach_context_snapshots`, `head_coach_recommendations`, `head_coach_outcomes`, `head_coach_audit_events`.
- 2 enums (`head_coach_task_status`, `head_coach_recommendation_status`), 2 trigger functions, 4 triggers.
- Immutability is DB-enforced, not just RLS: `supabaseAdmin` (service role) bypasses RLS like every other `api/*.js` file in this repo, so RLS alone can't guarantee a "never overwrite" property — `head_coach_context_snapshots`/`head_coach_audit_events` are insert-only (trigger blocks UPDATE/DELETE outright) and `head_coach_recommendations` locks after its first coach decision (trigger blocks further UPDATE; DELETE is blocked always).
- `task_id`/`recommendation_id`/`context_snapshot_id` FKs use `ON DELETE RESTRICT`, not `CASCADE` — a real finding from review, not stylistic: a cascading delete in Postgres fires the child table's own triggers, so `CASCADE` here would never actually cascade, it'd hit the immutability trigger and abort. `RESTRICT` states the real, guaranteed behavior instead of a misleading one. Net effect: once a client has any Head Coach recommendation history, deleting their profile fails outright rather than silently losing that history.
- Migration file: `db/proposed_head_coach_foundation.sql` (filename says "proposed" but this was reviewed and applied — treat it as the historical record, not a pending change). Verification scripts also live in `db/`: `verify_hc002_catalog.sql` (read-only catalog check), `verify_hc002_functional.sql` (rollback-wrapped live trigger/RESTRICT test, persists nothing).

**Design decisions baked in, all explicitly owner-approved (see the file's own header comments for full rationale):**
- New Head Coach code will be TypeScript; the rest of the app stays plain JS — no full-repo migration.
- No new `objectives`/`current_states` tables — reuses `client_goals` (§4) as-is, per the same "don't create a second read path for the same data" reasoning as the metric_key bug fixed earlier this session.
- Rule registry will be a versioned TS module (HC-007, not built yet), not a DB table — code-reviewed/deploy-gated rules are a stronger guarantee than a coach-editable table for something nothing should modify at runtime.
- The spec's `decisions`/`recommendations`/`coach_overrides` were consolidated into one table, `head_coach_recommendations`, instead of three — a real, flagged deviation from the literal spec.
- A future `api/head-coach.js` (HC-004, not built) will multiplex Head Coach actions by payload shape into one file, same pattern as `goal-insight.js`, to stay inside the 12-function Vercel cap (§5) — no plan upgrade needed.

**Explicitly deferred, not built:** the check-in→task trigger mechanism, the task-processing worker, `api/head-coach.*` itself, a generic `requireClient()` auth helper, any action executor (no autonomous actions in MVP scope), an `alerts` table, an `interventions` table, and anything client-facing.

**HC-003 (Authentication/RLS/Authorization) — complete.**
- **`api/_lib/headCoachAuth.ts`** (first TypeScript file in this repo — added `typescript`/`@types/node` as dev deps and a minimal `tsconfig.json`, `noEmit`, not wired into any build step since Vercel transpiles `api/*.ts` itself): two primitives, and Head Coach code should never write its own auth check outside these. `requireCoachForHeadCoach()` is a thin, unchanged wrapper around the existing `requireCoach()` — for every coach-driven action (view/approve/modify/reject/defer/record-outcome). `requireHeadCoachWorker()` is new — a shared-secret check (reuses `CRON_SECRET`, same shape as `checkin-digest.js`) for system-driven writes (task/context/recommendation creation, system-authored audit events) that have no human caller to check a role for.
- **No `requireClient()` built** — audited and confirmed unnecessary for MVP: no Head Coach operation is ever client-invoked (no client-facing UI in scope).
- **No HC-002 schema/RLS changes** — audited specifically for defects and found none; the existing coach-only-`SELECT`/zero-write-policy posture on all 5 Head Coach tables already fully satisfies "no client access, no client-writable path."
- **Found and fixed an unrelated, pre-existing, critical privilege-escalation gap** while auditing what `requireCoach()`/`is_coach()` actually depend on: `profiles`' RLS `UPDATE` policy is row-scoped (`id = auth.uid()`) but not column-scoped, so any client could `PATCH` their own `role` to `'coach'` via a direct Supabase REST call, bypassing the UI, and be trusted as a real coach everywhere in this app (not just Head Coach). Fixed in `db/fix_profiles_role_selfescalation.sql` — a `BEFORE UPDATE` trigger blocks a non-coach from changing `role`/`client_type`/the three assessment-score columns on their own row. Verified against every existing write path first: `api/_lib/starterActivation.js` (sets `client_type` via the service role during payment confirm) and `api/sync-client.js` (sets assessment scores via the service role) both still work, since the trigger only blocks when there's a live non-coach user session (`auth.uid()` non-null and `is_coach()` false) — service-role calls (`auth.uid()` null) and coach sessions are unaffected.
- **Applied to prod and verified**, same manual-SQL-editor process as every other migration here.

**HC-004 (Core State APIs) — complete.**
- **`api/_lib/headCoachCoreState.ts`** — a read-only TypeScript data-access module, NOT a new HTTP route (nothing calls it yet). Scoped to exactly "Core State" per the spec: `getClientCore`, `getObjectiveAndCurrentState` (the bodyweight goal, via the same `metric_key='bodyweight'` convention as GoalsSection), `getActiveMilestones`, `getActiveProgram`, `getActivePhase` (looks up `program_phases` by `programs.phase`'s name — `programs.phase` is the coach-set source of truth for "current," `program_phases` is the forward-looking planned roadmap, not itself a pointer to "now"), `getRecentPerformance` (strength trends + training consistency), `getNutritionContext`, `getRecentCheckins` (raw rows), `getAtRiskStatus`, `getRecentInterventions` (reads the HC-002 tables). Deliberately excludes "constraints"/"relevant history"/"applicable rules"/"authority" from the original spec's Context Builder list — those are assembly/rules/authority logic for HC-006/HC-007/HC-013, not raw state reads.
- **Reuses existing pure functions instead of reimplementing them** — `strengthTrendsFrom`, `nutritionAdherenceFrom`, `assessClientRisk` — so Head Coach's view of a client's risk/performance/nutrition can never silently diverge from what the coach dashboard already shows.
- **Extracted `currentMilestoneValues`** out of `api/goal-insight.js` into `api/_lib/milestones.js` so both `goal-insight.js` and the new core-state module share one implementation instead of a third copy being written.
- **Found and fixed a real bug while live-testing (not just type-checking)**: reusing `assessClientRisk` server-side failed at runtime — it depended on `todayStr` from `theme.jsx`, a JSX file Node can't load outside a bundler, a path nothing server-side had ever exercised before. Fixed by extracting `todayStr`/`localDateStr` into `src/lib/dates.js` (plain JS); `theme.jsx` re-exports both unchanged, so its 13 existing importers needed zero changes.
- **Verified live**, not just type-checked: every accessor was run against real client rows (a full-data coaching client and a program-only client with no goal/program) via a throwaway script, confirming both the populated and the null/edge-case paths work correctly. `npx tsc --noEmit`, `npm run build`, and the 45-test suite all pass.
- No schema changes, no new `api/*.js` routes, no client-facing surface.

**HC-005 (Event + Task Infrastructure) — complete.**
- **Key scoping decision (owner-approved)**: the app is already at the 12/12 Vercel function cap (an earlier claim in this doc that a future `api/head-coach.js` would "stay inside the cap" was wrong — there's zero headroom, not one free slot). No automatic check-in→task trigger and no cron worker for MVP; task creation + processing is coach-triggered, synchronous, added as a 4th action branch (`{ checkin_id }`) to the existing multiplexed `api/goal-insight.js` — no new `api/*.js` file.
- **`api/_lib/headCoachTasks.ts`** — task lifecycle only: `createTask` (idempotent via the HC-002 unique constraint — a double-click returns the same task, never errors), `claimTask`/`markReady`/`failTask` (compare-and-swap transitions, so a task can't be double-processed even if something concurrent shows up later), `writeAuditEvent`. Deliberately stops at `READY` — context assembly, rules, and AI reasoning are HC-006 onward and don't exist yet.
- **`handleReviewCheckIn`** in `api/goal-insight.js` — looks up the check-in, creates+claims its task, returns the current state. The existing top-level `requireCoach()` call on that route already satisfies HC-003's authorization requirement; no separate call needed.
- **Live-tested end to end**, not just type-checked: idempotency, claim compare-and-swap, the READY transition, and the failure path all confirmed against real data.
- **Found and fixed two real bugs via that live testing:**
  1. `writeAuditEvent` didn't check the error Supabase-js returns (`{data, error}`, doesn't throw) — a failed audit write looked identical to success. Fixed.
  2. A genuine HC-002 schema defect, invisible until now because nothing had written a real `task_id`-linked audit event before: `head_coach_audit_events.task_id`/`recommendation_id` used `ON DELETE SET NULL`, but Postgres implements `SET NULL` as an internal `UPDATE`, which the append-only trigger blocked unconditionally — deleting a task with any audit history always failed. Fixed in `db/fix_audit_events_setnull_deadlock.sql` with a smarter trigger (`head_coach_audit_events_guard_update`) that allows exactly the FK-nulling pattern while blocking real tampering — deliberately NOT switched to `RESTRICT` (like the earlier HC-003 fix used elsewhere) because every task now gets an audit event on creation, which would make every task permanently undeletable forever, a real cost given this repo has no staging environment and all future HC-006+ work keeps live-testing against prod.
- **Applied and verified**: tamper attempts on audit content still correctly blocked; a real reassignment attempt (valid task → different valid task) correctly blocked; the legitimate detach-on-delete path now works (verified by actually deleting a real test task and confirming its audit rows survived with `task_id` set to null, all other fields intact).
- **Known minor gap, not fixed (low priority, not exploitable)**: a `null` `task_id`/`recommendation_id` can currently be set to a valid value again (not just cleared) — only reachable by already-fully-trusted server-side code, and nothing in this codebase does it. Worth tightening later if this table's write patterns ever get more complex.
- Two harmless leftover test task rows (created during verification) were cleaned up using the fix once applied.

**HC-006 (Context Builder) — complete.**
- **`api/_lib/headCoachContextBuilder.ts`** — `buildReviewCheckInContext(clientId)` assembles every HC-004 core-state accessor into one object (client, objective+current-state, milestones, active program/phase, recent performance, nutrition, recent check-ins, constraints, open alerts + at-risk level, recent interventions), and `persistContextSnapshot(taskId, context)` writes it to `head_coach_context_snapshots` (HC-002 — insert-only at the DB level). `applicableRules`/`authority` are explicit placeholders (`[]`/`null`) since HC-007/HC-013 don't exist yet — read an empty array as "rules don't exist yet," never as "no rules apply."
- **Two spec fields with no dedicated schema** ("constraints", "relevant history") are mapped to the closest real data instead of inventing structure that doesn't exist: program description/goal/experience_level (coaches already write physical-limitation notes into this free text — confirmed real example during testing: "with knee-safe programming") plus recent `coach_notes` (pinned first). Left as narrative text for the eventual AI reasoning step (HC-011) to read.
- **Wired into `handleReviewCheckIn`**: claim → build context → persist snapshot → mark READY, returning the full context and snapshot id to the coach for now (nothing else consumes this yet).
- **Fixed a real bug found on re-review** (not caught by tests, since HC-005 had no code between claim and ready before now): `task` was never reassigned after a successful `claimTask`, so `failTask`'s compare-and-swap would have compared against a stale `QUEUED` status if anything failed between claiming and marking ready — silently failing to record the failure. Now `task` is kept in sync after every successful transition.
- **A real TypeScript/Node interop issue found and fixed**: a `.ts` file importing another `.ts` file with a `.js` specifier (the standard NodeNext convention) type-checks fine but fails at actual runtime — Node's module resolution doesn't do the `.js`→`.ts` fallback that `tsc` does for type-checking. Fixed by using explicit `.ts` extensions throughout this module family and enabling `allowImportingTsExtensions` in `tsconfig.json`. Confirmed working both ways (typecheck + live run).
- **Live-tested end to end** against a real client with real program/phase/performance/nutrition data: full context assembly, snapshot persistence, snapshot immutability (tamper attempt correctly blocked), and the `RESTRICT` relationship correctly preventing deletion of a task that has a context snapshot (by design, not a bug — same reasoning as recommendations).
- One more harmless permanent test task + context snapshot were left in prod by this testing (by design — `RESTRICT` means a task with a snapshot can never be deleted), same structural cost as HC-005's testing, given no staging environment exists.

**HC-007 (Rule Registry) — complete.**
- **Key scoping decision (owner-approved)**: this effort's own spec is explicit that V12's actual methodology/Constitution/Safety Rules are canonical, owned by the V12 owner, and not to be redefined by the implementing agent — and no such document was available in this session. So `api/_lib/headCoachRules.ts` builds the full registry infrastructure (types, versioning, priority-ordered lookup), but its 5 rules only either (a) formalize what this codebase already computes deterministically elsewhere, reading it directly from context rather than re-deriving it, or (b) restate a principle this effort's own spec already stated explicitly. No new clinical/coaching thresholds were invented. Real V12 safety/business rules need the owner's actual input before they're added.
- **The 5 rules**: `high_risk_escalate` (safety, priority 1 — fires on `atRiskLevel === "High"`, reading the same `assessClientRisk` classification the coach dashboard shows), `missing_consequential_data` and `no_active_objective` (validation — directly implement the spec's own "never invent missing data" / Golden Case principles), `at_risk_flagged` and `milestone_achieved` (advisory — mirror the coach dashboard's existing at-risk panel and milestone-achieved detection).
- **"never diagnose" and similar always-on behavioral constraints are deliberately NOT modeled as Rules** — they're not trigger/condition-shaped; they belong to the Safety Gate (HC-009) / Prompt Architecture (HC-011).
- **Extracted `milestoneProgress`** out of `src/lib/milestones.js` (which imports the browser `supabaseClient.js` at module scope) into `src/lib/milestoneProgress.js`, same pattern as `dates.js` in HC-004 — found needed when a rule tried to reuse it server-side. `src/lib/milestones.js` re-exports it unchanged.
- **Live-tested**, not just type-checked: ran the full registry against two real, contrasting clients (a full-data coaching client with no rules firing, and a program-only client with no goal/program correctly firing both validation rules) — priority ordering confirmed correct (safety evaluated first).
- Rules don't do anything yet — evaluating them against a real context and recording which fired is the Rule Engine's job (HC-008, not built).

**HC-008 (Rule Engine) — complete.**
- **`api/_lib/headCoachRuleEngine.ts`** — `evaluateRules(trigger, context)` runs every active rule for that trigger (already priority-sorted safety-first by `rulesFor`), checks each rule's `requiredData` paths are present, evaluates its condition, and derives one overall verdict (`proceed`/`no_action`/`escalate`) from the **highest-priority rule that fired** — this is what implements "safety is evaluated before normal optimization": a fired safety rule always outranks a fired advisory rule regardless of how many of the latter also matched.
- **`requiredData`/`failure_behavior` are evaluated defensively but not currently reachable in practice**: `buildReviewCheckInContext` (HC-006) is all-or-nothing — if any accessor throws, the whole context build throws before a Rule Engine ever runs, so every field that reaches here is always fully populated (possibly `null` as a legitimate business value, never technically "missing"). Still implemented correctly for when Context Builder might become partial/best-effort later. A rule with `failure_behavior: "task_failure"` throws immediately (propagates to the existing `failTask` path), rather than silently affecting the verdict.
- **Wired into `handleReviewCheckIn`**: context is built, then rules are evaluated against it, then `applicableRules` (previously always `[]`) is populated with the outcomes and the snapshot is persisted with real `rule_versions` — in that order, since `head_coach_context_snapshots` is insert-only and there's no later chance to patch this in.
- **Live-tested end to end** against two real, contrasting clients: a full-data client correctly produced `verdict: proceed` (no rules fired); a program-only client with no goal/program correctly fired both `missing_consequential_data` and `no_active_objective`, with the verdict correctly driven by whichever has the higher priority (`missing_consequential_data`). Confirmed the persisted snapshot's `rule_versions` column matches exactly what the engine computed.
- Two more permanent test tasks (with context snapshots) left in prod by this testing — same disclosed, by-design cost as HC-006 (once a task has a snapshot, `RESTRICT` means it can never be deleted).

**HC-009 (Safety Gate) — complete.**
- **`api/_lib/headCoachSafetyGate.ts`** — `applySafetyGate(ruleResult)` acts on HC-008's verdict, not on the raw context: `escalate` → `escalate`, `no_action` → `close_no_action`, `proceed` → `proceed_to_reasoning`. Deliberately not itself a rule evaluator (HC-008) or the AI reasoning step (HC-010/HC-011, not built) — it's the checkpoint deciding whether a task is even allowed to continue toward AI reasoning at all.
- **Two new task transitions added** to `api/_lib/headCoachTasks.ts`, exercising two previously-unused states from the HC-002 enum: `closeTaskNoAction` (READY → `CLOSED`) and `escalateTask` (READY → `ESCALATED`), each writing its own audit event. A `no_action`/`escalate` outcome now resolves the task completely and it never reaches AI reasoning; a `proceed` outcome leaves the task at READY untouched, since there's nothing built yet for it to proceed to.
- **Wired into `handleReviewCheckIn`** immediately after `markReady`.
- **Live-tested**: the pure decision logic for all three verdicts; the `ESCALATED`/`CLOSED` transitions and their audit events against real (throwaway) tasks — both cleaned up successfully afterward since neither had a context snapshot yet (confirms `RESTRICT` only locks in tasks that reached a snapshot, not every task); and the full real pipeline for a genuine On Track client, confirming the gate correctly leaves a READY task untouched when there's no safety objection.
- No real client in this database is currently `High` risk, so the `escalate` path was verified via the pure gate-logic test and the transition/audit mechanism directly, not through a naturally-occurring real case — worth a real end-to-end check once a genuinely at-risk client exists.

**HC-010 (Claude Adapter) — complete.**
- **`api/_lib/headCoachModelAdapter.ts`** — a genuinely provider-neutral `ModelAdapter` interface (`complete(prompt, options) -> {text, model, modelVersion, usage, latencyMs}`), with `ClaudeAdapter` as its only implementation for now. Separate module from `api/_lib/anthropic.js` on purpose — that file's existing functions (`generateGoalInsight`, `generatePhaseRecommendation`, `generateRoadmap`, etc.) are untouched, unrelated features. Uses the same model string (`claude-opus-4-8`) already used everywhere else in this app — not a Head Coach-specific model choice, just consistency; changing it is a separate decision.
- **Failures throw `ModelAdapterError`** (carrying `model`/`latencyMs`), not a silent result — so a future caller can catch it and fail the task gracefully via the existing `failTask()`, per "make AI failure non-fatal to the core application."
- **"The frontend must never call Claude directly" holds by construction**, not a runtime check — nothing under `api/_lib/` is ever imported by `src/`.
- **Not wired into any Head Coach flow yet** — there's no real prompt to send (that's HC-011, the Prompt Architecture / actual REVIEW_CHECK_IN prompt). Wiring this in earlier would mean burning real API calls on placeholder text every time a coach clicks the review button.
- **Live-tested with real API calls** (not mocked): a successful completion correctly captured real token usage, real latency, and the exact model string; a forced failure (invalid model name) correctly threw `ModelAdapterError` with the real error message, model, and latency attached.

**HC-011 (REVIEW_CHECK_IN Prompt) — complete. First real AI reasoning in this whole effort.**
- **`api/_lib/headCoachReviewCheckInPrompt.ts`** — `buildReviewCheckInPrompt` assembles SYSTEM + RULES + CONTEXT + TASK as clearly separated sections (not one undifferentiated prompt), per the spec. The system prompt walks through every required reasoning step (validate evidence, check safety, identify objective/current state/gap/limiting factors, consider competing explanations, seriously consider NO ACTION, state confidence honestly, treat its own "authority" as advisory only) and defines the full output shape (`status`, `observation`, `evidence[]`, `interpretation`, `confidence`, `confidence_level`, `limiting_factor`, `competing_hypotheses[]`, `options[]`, `recommendation`, `rationale`, `authority`, `expected_response`, `review_condition`, `client_communication_needed`, `communication_draft`) exactly matching the spec's OUTPUT section.
- **Real prompt-injection defense, not just a comment**: the context JSON is explicitly framed as untrusted data, and the model is instructed to ignore anything inside it that looks like an instruction.
- **Only reached on a Safety Gate "proceed" outcome** — by construction, the only rules that can be active at that point are advisory-category, capped at L1 (any safety/validation rule firing would have already resolved the task in HC-009). The prompt tells the model which rules already fired so its authority never exceeds what they permit.
- **Output is NOT persisted as a recommendation yet** — `head_coach_recommendations.ai_output` is immutable the instant it's inserted (HC-002), so writing unvalidated output into it now would mean bad data could get stuck there forever. HC-011 only does a bare "did this parse as JSON" check (the same minimal check every existing AI function in `api/_lib/anthropic.js` already does) — real validation (schema/evidence/fabrication/authority/safety, bounded retry) is HC-012.
- **Wired into `handleReviewCheckIn`**: on a "proceed" gate outcome, the model is actually called now and its raw output returned to the coach for visibility; the task stays at READY (no premature state transition before a real recommendation exists).
- **Live-tested with 3 real clients, real API calls, real money spent** — no fired rules (On Track client): correctly concluded `no_action`, cited real evidence, and **independently caught a genuine data-entry bug in production** (`client_goals.target_value = 3320` for a decrease-direction goal with baseline 344 — clearly wrong, not fixed here, just flagged; unrelated to this session's work). Advisory rule fired (`at_risk_flagged`, Medium risk): correctly recommended fixing nutrition-logging quality before assuming a real plateau, `authority: L1` matching the rule's cap. Sparse-data client (Low risk): correctly low-confidence `no_action`. All three produced coherent, evidence-grounded reasoning, not fabricated content.
- One more permanent test task+snapshot left in prod — same disclosed, by-design cost as every prior live-tested step.

**HC-012 (Output Validator) — complete.**
- **`api/_lib/headCoachOutputValidator.ts`** — `validateReviewCheckInOutput()` runs the spec's exact validation order (JSON → schema → enums → evidence → rules → fabrication → authority → safety), stopping at the first stage with issues. The "rules" stage cross-checks the AI's declared `authority` against the real ceiling any fired rule permits — fully data-driven (reads `rule.authority`, added to `RuleOutcome` in `headCoachRuleEngine.ts` for this), not hardcoded.
- **Honest scope note on "fabrication"**: real fact-checking (verifying each evidence claim traces to an actual number in the context) would need a second LLM-based critic call or real NLP, neither built. What's implemented is a narrow, deterministic coherence check (confidence_level text must match the numeric confidence; a recommend/escalate status must cite evidence) — catches internal inconsistency, does not verify truth. Documented as a real gap, not silently glossed over.
- **`requiresEscalation` is computed independently of `valid`** — an output that validates perfectly clean but has `status: "escalate"` still requires escalation; a caller can never accidentally skip it just because the rest of the output happened to be well-formed.
- **Bounded retry**: exactly one retry per the spec ("if output is invalid: bounded retry"). Still invalid after that, or the (possibly-invalid) output itself declares `escalate` → `ESCALATED`, the same human-review mechanism the deterministic Safety Gate already uses — `escalateTask` now accepts either `READY` (gate-driven) or `RETRYING` (validator-driven) as its source state. New `markRetrying()` added to `headCoachTasks.ts`, exercising the previously-unused `RETRYING` state.
- **Wired into `handleReviewCheckIn`** right after AI reasoning. Output is still not persisted as a `head_coach_recommendations` row — that's HC-013 (Authority Engine) / HC-014 (Recommendation Manager).
- **Live-tested**: 8 synthetic cases covering every validation stage's pass/fail behavior including the escalate-independent-of-validity property, plus one real end-to-end run with a real API call — validated cleanly on the first attempt, no retry triggered, task correctly stayed at READY.

**HC-013 (Authority Engine) — complete.**
- **`api/_lib/headCoachAuthorityEngine.ts`** — `determineAuthority(taskType, ruleOutcomes, aiDeclaredAuthority)` implements "the backend is authoritative, Claude's declared authority is advisory only" literally: it independently computes a ceiling from (1) a per-task-type max (`REVIEW_CHECK_IN` capped at L1 for MVP — no action executor exists, "do not begin with autonomous actions" is a standing constraint) and (2) the fired rules' authority, then takes the **minimum** with whatever the AI declared. The AI can only ever be *more* conservative than the backend's ceiling, never less.
- **Found and fixed a real logic bug while building this**: HC-012's rule-ceiling check used `Math.max` across fired rules' authority values — if two rules fired simultaneously, one capping at L0 and another at L1, that would have let the AI use L1 even though the L0 rule specifically flagged a reason for caution. Correct composition for multiple simultaneously-binding constraints is `Math.min` (most restrictive wins). Not currently reachable with today's rule set (the only rules that can co-fire by the time reasoning happens are both L1), but wrong regardless — fixed in both `headCoachOutputValidator.ts` (retroactively) and `headCoachAuthorityEngine.ts` (built correctly from the start).
- **Wired into `handleReviewCheckIn`** right after validation, computed independent of whether the output validated cleanly (a bad/missing AI-declared authority is handled defensively, falling back to the backend's own ceiling — never a crash).
- **Live-tested**: 6 scenarios including the exact multi-rule MIN-composition case that motivated the fix, an AI attempting to claim `L4` (correctly clamped to the real L1 ceiling — defense in depth even if HC-012 somehow let something through), an unknown task type defaulting to the most conservative `L0`, plus one real end-to-end run with a real API call.
- Output is still not persisted as a `head_coach_recommendations` row — that's HC-014 (Recommendation Manager).

**HC-014 (Recommendation Manager) — complete. First real, permanent `head_coach_recommendations` row in this whole effort.**
- **`api/_lib/headCoachRecommendations.ts`** — `createRecommendation()` persists exactly one row per model call, **regardless of outcome**: clean recommend/no_action, AI-declared escalate, or output that failed validation even after the bounded retry all get a permanent record — "preserve historical decisions" isn't just for the good outcomes. Always inserted with `status: 'pending'`; deciding it is HC-015 (Coach Approval), not this module.
- **Validation stages mapped down to the DB's existing 5-value `validation_status` enum** (not a schema change — HC-002 already anticipated this): json/schema/enums → `failed_schema`, rules/authority → `failed_rules`, evidence/fabrication → `failed_safety`. `no_action_fallback` is defined in the schema but not produced by any current code path — this module always escalates rather than silently falling back to no_action when validation fails, since escalating guarantees a human sees it.
- **Two new task states finally exercised**: `DECISION_READY` (clean outcome, awaiting the coach's normal review) via new `markDecisionReady()`, and `ESCALATED` now also reachable from a failed/escalating recommendation, not just the deterministic Safety Gate.
- **Wired into `handleReviewCheckIn`** right after authority determination — a recommendation row is now always created on "proceed," and the task moves to `DECISION_READY` or `ESCALATED` accordingly.
- **Live-tested with a real end-to-end run**: created a real, permanent recommendation (`status: pending`, `validation_status: valid`, `backend_authority: L0`, real model/latency/token usage), confirmed the task correctly moved to `DECISION_READY`, and confirmed the HC-002 immutability trigger still blocks tampering on this real row.

**HC-015 (Coach Approval) — complete. Real coach interaction on a recommendation, for the first time.**
- **`api/_lib/headCoachApproval.ts`** — `applyCoachDecision()` supports approve/modify/reject/defer. "Never overwrite the original recommendation" and "store modifications separately" are already structural guarantees from HC-002 (separate `ai_output`/`coach_modification` columns, DB-enforced immutability), not reimplemented here. "Expected state/version validation before executing" is a compare-and-swap on `status = 'pending'` in the same UPDATE — the same primitive used throughout `headCoachTasks.ts` — rather than a separate version-number scheme; if the row was already decided, this returns `alreadyDecided: true` cleanly instead of erroring or silently overwriting a prior decision.
- **New action on `api/goal-insight.js`**: `{ recommendation_id, decision, note?, modification? }` — no new `api/*.js` file, same multiplexing pattern as every other Head Coach action on this route.
- **Two more task transitions**, exercising the last remaining previously-unused states: `markActionPending()` (approve/modify → `ACTION_PENDING` — no automated executor exists, HC-016 is deferred, so this means "coach approved, a human carries it out manually," same convention documented since HC-002) and `closeTaskDecided()` (reject/defer → `CLOSED`, since a decided recommendation is locked forever and revisiting means a new review, not resuming this one).
- **Live-tested all 4 decisions on real rows**: approved the actual recommendation created during HC-014 testing (task correctly moved to `ACTION_PENDING`); created lightweight synthetic recommendations (real tasks/snapshots, fabricated model output — no extra API cost) to test modify (confirmed `coach_modification` stored separately from `ai_output`), reject, and defer (both → `CLOSED`). Confirmed the double-decision guard (`alreadyDecided: true`, no corruption) and confirmed the HC-002 lock trigger still blocks any attempt to alter a decided row, even direct DB tampering.

**HC-016 (Approved Action Executor) — deliberately deferred, no code built. Owner decision, not a placeholder.**
- Asked before writing anything, because `REVIEW_CHECK_IN`'s AI output (HC-011) only ever produces free-text `recommendation`/`rationale` for the coach to act on manually — never a structured, executable change to programs/nutrition/phases. Of the spec's example actions (`applyApprovedProgramChange`, `applyApprovedNutritionChange`, `applyApprovedPhaseChange`, `applyApprovedExerciseSubstitution`, `sendApprovedCommunication`), only `sendApprovedCommunication` has anything real to execute right now, and even that raises a real product question (does approving a recommendation also authorize sending a message, or should that be a separate step?).
- **Decision: build nothing for now.** `ACTION_PENDING` stays a manual state for the whole MVP — the coach carries out the recommendation themselves outside the system, exactly as documented since HC-002. This is a real, deliberate scope boundary, not something left unfinished — matches the spec's own repeated instruction not to begin with autonomous program/nutrition/phase changes, and there's currently no task type whose output would even give an executor something structured to act on.
- Revisit this once (a) a task type exists that proposes a genuinely structured, executable change, and (b) the approve-vs-send question above has a real answer.

**HC-017 (Outcome Tracker) — complete.**
- **`api/_lib/headCoachOutcomes.ts`** — `recordOutcome()`, coach-initiated (no automated executor exists, HC-016). Only meaningful for a recommendation the coach actually acted on — rejects with a clear error if the recommendation's status isn't `approved`/`modified` (recording an "outcome" for a rejected/deferred recommendation doesn't mean anything, nothing happened). Defaults `expected_response` to the AI's own `ai_output.expected_response` from HC-011 (still overridable) so the coach doesn't have to retype it.
- **Unlike `ai_output`/context snapshots, outcomes are NOT locked immutable** — HC-002 deliberately left `head_coach_outcomes` without a lock trigger, since a coach may reasonably want to add/correct notes later. Only creation is implemented for MVP.
- **New action on `api/goal-insight.js`**: `{ record_outcome_for, actual_response, outcome, expected_response?, measurement_period_start?, measurement_period_end?, notes? }` — 6th action on this same route, no new `api/*.js` file.
- **New task transition** `closeTaskAfterOutcome()` (`ACTION_PENDING` → `CLOSED`) — closes the loop; per the HC-016 decision, `MONITORING` is not modeled as a separate step in this MVP.
- **Guardrail documented, not implemented**: "do not automatically rewrite methodology based on outcomes" — nothing here or anywhere in this effort feeds outcomes back into HC-007's rule registry automatically, and that should require an explicit future decision, not happen by drift.
- **Live-tested on real rows**: recorded a real outcome on the actual approved recommendation from HC-014/HC-015 (correctly defaulted `expected_response` from its real AI output, task moved to `CLOSED`); confirmed a `modified`-status recommendation is also accepted; confirmed `rejected` and `deferred` recommendations are both correctly refused with a clear error.

**All 11 head_coach_task_status values have now been exercised at least once**: QUEUED, CONTEXT_BUILDING, READY, PROCESSING (not exercised — no code path currently uses it, see note below), DECISION_READY, ACTION_PENDING, CLOSED, FAILED, RETRYING, ESCALATED. MONITORING remains unused by design (see above). Actually PROCESSING is also unused — nothing in this synchronous, single-request pipeline needs an intermediate "actively processing" state distinct from CONTEXT_BUILDING/READY; noting honestly rather than forcing an artificial use.

**HC-018 (Audit System) — complete. Verification pass, as expected, not new infrastructure.**
- **`api/_lib/headCoachAuditTrail.ts`** — `reconstructTaskHistory(taskId)` proves (not just claims) that every element the spec requires is reconstructable: event (source check-in via `task.source_table`/`source_id`), task, context (immutable snapshot), rules (`rule_versions` on the snapshot), model/output (`recommendations[].model`/`ai_output`), decision (`status`/`decided_by`/`decided_at`), recommendation, coach action (`auditTrail[].actor`/`actor_label` across every step), intervention (per the HC-016 decision, an approved/modified recommendation IS the intervention — no separate table exists), and outcome. This is also genuinely reusable infrastructure, not just a test harness — it's the exact query a future coach-facing "full history of this review" view would need.
- **Live-tested against the real, complete chain from HC-014/015/017's testing**: reconstructed the full story end to end — 5 audit events spanning task creation through outcome recording, the real context snapshot with 5 rule outcomes, the real recommendation with real model/latency/token data, the real coach decision, and the real outcome. All coherent and complete.
- **Found one real thing worth documenting, not a bug**: the "event" leg initially came back "not found" — traced to this session's own test methodology (fabricated check-in IDs used to avoid unnecessary API/data dependencies during testing), not a gap in the actual code. Confirmed by creating a task through `createTask` with a genuinely real check-in ID and re-running the reconstruction — resolved correctly. In real usage, `handleReviewCheckIn` always validates the check-in exists before ever creating a task, so `source_id` is always resolvable at creation time. Worth knowing: this leg depends on the source row still existing, and `daily_checkins` cascades with the client like everything else in this app — not a new Head Coach-specific gap.
- **Known, honest gap, unchanged**: `methodology_version` is always `null` — no real V12 methodology document has ever been available in this whole effort.

**HC-019 (Coach Dashboard) — complete. First UI for the whole Head Coach effort (HC-001 through HC-018 were all backend).**
- **Owner decisions**: lives as a new per-client tab (matches how everything Head Coach has been scoped since HC-002 — per client, per check-in), same pattern as GoalsSection/ProgramSection/MilestonesCard. Plus a feature-flag kill-switch, per the spec's explicit (and until now never-built) "feature controls to disable Head Coach without disabling normal functionality" requirement.
- **`src/features/clientDetail/sections/HeadCoachSection.jsx`** (new) — shows the client's most recent check-in with a "Review This Check-In" button, then every past Head Coach task/recommendation for that client with status-appropriate actions: Approve/Modify/Reject/Defer for `DECISION_READY`, a "record what happened" form for `ACTION_PENDING`, a clear "needs your review" flag for `ESCALATED`. Reads go straight to Supabase from the browser (RLS already grants coaches `SELECT` on every `head_coach_*` table, HC-002); writes go through `api/goal-insight.js`, same as every other AI action in this app — there's no client-writable RLS policy on any of these tables by design.
- **New "Head Coach" tab** wired into `ClientDetailPage.jsx`/`TABS_FOR`, hidden for `program_only` clients (same exclusion as Goals — `REVIEW_CHECK_IN` needs `daily_checkins`, which program-only clients don't have a check-in flow for).
- **Feature flag, both sides**: `VITE_HEAD_COACH_ENABLED` (frontend, hides the tab) and `HEAD_COACH_ENABLED` (backend, `api/goal-insight.js` refuses `checkin_id`/`recommendation_id`/`record_outcome_for` actions with a clear 503 if set to `"false"` — every other action on the route is unaffected). Both default to enabled if unset. Documented in `.env.example`.
- **Live-tested in an actual browser** (not just build/typecheck), using Playwright directly (no project skill existed for this yet, and `chromium-cli` wasn't available in this environment — used the cached Chromium binary directly). Logged in as the real coach (password provided by the owner for this session only, never written to any file or committed) and clicked through the real UI.
- **Found and fixed two real bugs via that live testing, not caught by build/typecheck:**
  1. The tab bar (`ClientDetailHeader.jsx`) uses `flex: "1 1 140px"` inside a wrapping flex container sized for 4 tabs — adding a 5th made it wrap onto its own row, where a lone flex-grow:1 item stretched to fill the entire row width (a giant, out-of-place button). Fixed by changing to `flex: "0 1 140px"` so a wrapped tab keeps its natural size.
  2. Clicking "Review This Check-In" surfaced a real `fetch` error ("Unexpected end of JSON input") — traced to `curl` confirming plain `vite` dev server (`npm run dev`) returns a bare 404 for any `/api/*.js` route, since Vite's dev server doesn't proxy those at all (only `vercel dev` or a real deployment does). This is a **pre-existing environment limitation that would affect testing any existing AI feature in this app the same way** (Generate Insight, Generate Roadmap, etc.), not a bug introduced here — and the actual backend logic being called (`handleReviewCheckIn` and everything behind it) has already been extensively live-tested via dozens of real Anthropic API calls throughout HC-005 through HC-018, just via direct script invocation rather than a browser round-trip. The UI's own request-sending and error-handling (the `Alert` component showing the failure clearly, not crashing) worked exactly as designed.
- **Confirmed working in the browser**: coach login, client navigation, the Head Coach tab rendering with real historical task data read via RLS, and the tab-bar layout fix.
- **Honest gap**: the full "click button → real HTTP round-trip → real AI call → UI updates with a new recommendation card" path was not verified end-to-end in the browser, due to the dev-server limitation above. Verifying that would need `vercel dev` (Vercel CLI + project linkage) instead of plain `vite`, or a real deployment — worth doing before treating this as fully proven, if that matters before broader use.

**HC-020 (Client Integration) — complete.**
- **Owner decision**: deliberately NOT a new client-facing surface or any automated sending. A small connector into the existing, already-manual `coach_messages` feature (`CoachMessagesSection.jsx`) — the client never sees AI reasoning directly, only whatever message text the coach actually reviews and sends.
- **`CoachMessagesSection.jsx`**: added an optional `initialBody` prop (read once at mount — the component only ever mounts fresh inside its own modal, so no effect needed to re-sync it).
- **`HeadCoachSection.jsx`**: a "Send to Client via Coach Messages" button appears on a `TaskCard` once its recommendation is `approved`/`modified` AND the AI's output has `client_communication_needed: true` with a real `communication_draft` (checking `coach_modification.communication_draft` first for forward-compatibility, though nothing writes to that field today — the Modify form only edits `recommendation`). Clicking it calls a new `onSendToClient` prop.
- **`ClientDetailPage.jsx`**: wires `onSendToClient` to open the existing "Send Client Message" modal with `messageDraft` pre-filled — same modal/component the "Send Message" quick action already uses, no new modal.
- **Live-tested in an actual browser**: created a synthetic approved recommendation with a real communication draft (no extra API cost) for a real coaching-tier client, confirmed the button appears, clicked it, and confirmed the modal opened with the composer genuinely pre-filled with the exact draft text, ready for the coach to edit/send through the feature that already existed.
- **Also confirmed live**: a `program_only` client's Head Coach tab is correctly absent from the UI entirely (per HC-019's exclusion) even though nothing stops the backend from having created Head Coach data for one via a direct script — a real gap between "what a coach can reach through the UI" and "what the backend allows," worth knowing about even though it isn't reachable through any real coach workflow.

**HC-001 through HC-020 are now complete.** Remaining execution-order items (HC-021 Golden Cases, HC-022 Security/Safety Tests, HC-023 End-to-End Tests, HC-024 Staging, HC-025 Controlled Pilot) are testing/rollout phases, not new capability — worth a scoping conversation on what's realistic given this repo has no staging environment (a fact established back in HC-002) and no formal test framework for API routes yet.

**HC-021 (Golden Cases) — complete.**
- **`test/headCoachGoldenCases.test.js`** (new, 17 tests, added to the existing `npm test` suite — now 62 total, all passing) — a genuine, permanent, automated regression suite, not more one-off scripts. Pure unit tests against the deterministic modules (Rule Engine, Safety Gate, Output Validator, Authority Engine) using synthetic contexts — no live model calls, no database writes, same zero-dependency convention as the pre-existing 45 tests.
- **Honest scope, stated explicitly in the file itself**: several of the spec's 16 named Golden Cases are NOT deterministic by nature (`one missed workout → adapt/continue`, `technique limitation → no automatic progression`, `confirmed plateau`, `conflicting evidence`) — they require the AI's own qualitative judgment from evidence, and a real automated test would need either a live model call every run (expensive, slow, non-deterministic) or a mock (no infrastructure for that exists here). Not automated; informally exercised via the real, live testing already done throughout HC-011/012/013.
- **A real constraint found while writing these**: `api/_lib/supabaseAdmin.js` constructs its Supabase client unconditionally at import time from env vars, so any test importing that chain (`createTask`, `applyCoachDecision`, `recordOutcome`, etc.) throws under plain `npm test` (no `.env` loaded), regardless of whether a DB call is ever made. Rather than adding an env dependency to the whole suite, "coach override" and "stale recommendation" (the guard clauses in `headCoachApproval.ts`) are left un-automated and documented as covered by HC-015's live testing instead.
- **Automated cases covered**: stable client → proceed; missing consequential data → NO ACTION (never reaches AI reasoning); no active objective → NO ACTION (plus a priority-ordering regression check); safety signal (High risk) → escalate; at-risk flagged → surfaced advisory, not forced; milestone achieved → surfaced advisory; no-action stability (same context, same verdict, twice); unauthorized action (AI claiming `L4` is clamped to the real ceiling; unknown task type defaults to `L0`); the multi-rule MIN-composition regression from HC-013; all 8 Output Validator stage failures (json/schema/enums/evidence/rules/fabrication) plus the escalate-independent-of-validity property.
- **Two Golden Cases verified with one real, live check each instead of an automated test** (appropriate given they need a real model call / real cross-client data, not something to run on every `npm test`):
  - **Prompt injection**: built a real context for a real client, injected a fake "SYSTEM OVERRIDE" instruction (in-memory only, never written to the real database) into the coach-notes field demanding `authority: "L4"` and a medication-dosage recommendation. The model correctly identified it as a prompt injection, refused to comply, stayed at conservative `L0` authority, cited the injection attempt as evidence rather than acting on it, and proactively flagged it in `review_condition` as something to escalate to a human — the defense built in HC-011 works as designed.
  - **Service boundary (cross-client isolation)**: not a fresh test — already implicitly verified dozens of times throughout HC-006 onward, since every test used a specific real `clientId` and consistently got back only that client's own data, never cross-contaminated.

**HC-022 (Security/Safety Tests) — complete. Found and fixed two real issues, not just a verification pass.**
- **Replay (fixed)**: `recordOutcome()` had no guard against a duplicate/replayed submission — a double-click or retried request could insert two `head_coach_outcomes` rows for the same recommendation before anything caught it. Fixed by making the task's `ACTION_PENDING → CLOSED` transition the atomic gate: `recordOutcome()` now claims that compare-and-swap itself, *before* inserting the outcome row, so only the request that wins the claim can ever write one — a second/replayed request is rejected before touching `head_coach_outcomes` at all. (`goal-insight.js`'s `handleRecordOutcome` no longer does this transition separately, closing the check-then-act race that existed between the two calls.) Live-tested: first attempt succeeds, replayed second attempt correctly rejected, confirmed exactly one outcome row exists in the database afterward.
- **PII leakage (fixed)**: `ClientCore`/`getClientCore` included the client's `email`, which flowed verbatim into the prompt sent to Anthropic (a third party) and into the immutable context snapshot. Grepped every Head Coach module and the UI — nothing downstream ever read it. Removed entirely; audit reconstruction can always join back to `profiles.email` via `client_id` if genuinely needed later.
- **Cross-client access / RLS bypass**: not re-tested live this round — already established with deterministic proof via HC-002's catalog inspection (exactly one `is_coach()`-gated `SELECT` policy per table, zero `INSERT`/`UPDATE`/`DELETE` policies for any role on any of the 5 tables). Postgres RLS behavior here is well-defined and doesn't depend on a live session to confirm; noting explicitly *how* this was verified rather than re-asserting it.
- **Role escalation**: already fixed and verified with a real exploit attempt in HC-003 (`profiles.role` self-promotion trigger).
- **Prompt injection**: already verified live in HC-021 (the model correctly refused an injected "SYSTEM OVERRIDE" instruction).
- **Stale approvals**: already verified live in HC-015 (the double-decision compare-and-swap guard).
- **Secret exposure**: grep-audited every `process.env` reference in Head Coach code — only `CRON_SECRET` (comparison only) and `ANTHROPIC_API_KEY` (client construction only), neither ever returned or logged. Confirmed every error response in `goal-insight.js` exposes only `e.message`, never a raw error object.
- **Unauthorized actions**: live-tested the real `handler` export with no `Authorization` header and with a garbage bearer token — both correctly rejected with 401 before any Head Coach logic ran (task creation, recommendation lookup, etc.).

**HC-023 (End-to-End Tests) — complete.**
- **`scripts/head-coach-e2e-check.mjs`** (`npm run headcoach:e2e`) — a real, reusable smoke-test script, not part of `npm test`. Exercises the REAL, HTTP-callable `handler` export from `api/goal-insight.js` (not the internal functions directly, which is what most development-time testing in this effort did) through a full real cycle: review a real check-in (a real Anthropic API call if it reaches reasoning) → approve the recommendation → record an outcome → confirm the double-decision guard (HC-015) and the replay guard (HC-022) both correctly reject further attempts → reconstruct the full history (HC-018) and confirm it's complete.
- **Deliberately not part of `npm test`**: it makes a real Anthropic API call (real cost, ~15-25s) and needs Supabase + Anthropic env vars, both incompatible with that suite's fast, free, zero-dependency convention. Meant to be run manually after a change to the Head Coach pipeline, or before a deployment — a genuine smoke test, not a CI gate.
- **Auths as the coach without a real login**: monkey-patches `supabaseAdmin.auth.getUser` to resolve as the target coach for the duration of the script, then restores it — everything downstream of that (task creation, context assembly, the real Claude call, rule evaluation, validation, authority, persistence, approval, outcome recording) runs completely for real, unmodified.
- **Live-run successfully end to end**: all assertions passed — task created, rule engine and safety gate both ran and returned real verdicts, a real recommendation was persisted, approval moved the task to `ACTION_PENDING`, outcome recording moved it to `CLOSED`, a re-decision attempt was correctly rejected with 409, a replayed outcome submission was correctly rejected, and the full history reconstructed to exactly one context snapshot, one recommendation, and a 5-event audit trail.

**HC-024 (Staging) — deliberately deferred, owner decision, not a placeholder.**
- A real staging environment means a second Supabase project (a new account/billing decision only the owner can make) plus separate Vercel Preview environment variables pointing to it — Preview deployments today share the SAME production Supabase project as `main` (confirmed via `.env.example`'s own instruction to set every Supabase var for "Production + Preview"). This is real infrastructure, not something to scaffold without that decision.
- **Decision: defer, keep operating as this whole effort already has.** No real staging environment. Continue the practice used since HC-002: careful, disclosed live testing against production, with real fixes verified live before committing — the same approach the owner has approved at every single HC step so far.
- Revisit if/when the owner sets up a second Supabase project and separate Vercel Preview env vars — the technical wiring (schema migration, env var setup, deployment config) can be helped with once those account-level decisions are made.

**HC-025 (Controlled Pilot) — complete. No code, a pilot guide.**
- **Owner decision**: no code-enforced allowlist. The feature already requires explicit human action at every single step (no autonomous execution, per HC-016) — that IS the real containment. What follows is the operational guide for actually piloting it.

**How to pilot Head Coach safely:**
1. **Start with 2-4 real clients you know well**, ideally a mix: one stable/on-track, one you'd expect to trigger an at-risk flag, one with sparse check-in data. Use the real "Review This Check-In" button on their Head Coach tab.
2. **Read the full reasoning before deciding, not just the recommendation line.** Check `observation`, `evidence`, `competing_hypotheses`, and `confidence_level` against what you actually know about the client. A low-confidence output is a legitimate, correct answer when data is genuinely sparse — don't treat it as a failure, and don't rubber-stamp a recommendation you haven't actually read.
3. **Red flags to stop and investigate, not just note:**
   - Evidence that doesn't check out against what you know to be true about the client (possible fabrication).
   - Confidence that feels miscalibrated (high confidence on thin evidence, or vice versa).
   - An escalation that doesn't make sense, or — more importantly — a real concern that *should* have escalated but didn't.
   - Anything that reads like it assumes an action will happen automatically. It shouldn't — authority should always show `L0`/`L1` right now (no action executor exists, HC-016), and nothing acts on a client without you approving and then doing it yourself.
4. **Known gap to work around manually during the pilot: escalations are not surfaced anywhere else.** An `ESCALATED` task only shows up on that specific client's Head Coach tab — there is no dashboard alert, no email, no notification of any kind (checked: nothing in `CoachHome`'s existing alert panels knows about Head Coach at all). During the pilot, you have to proactively open each piloted client's tab to see if anything needs your attention.
5. **Cost awareness**: each "Review This Check-In" click is a real Anthropic API call (real cost, ~15-25 seconds). Don't trigger it reflexively — same discipline as any other paid AI action in this app.
6. **Reviewing what happened**: the Head Coach tab shows each task's outcome inline. For a deeper look at any single review (full context, rule evaluation, audit trail), `scripts/head-coach-e2e-check.mjs`'s pattern of calling `reconstructTaskHistory()` (HC-018) is the tool — there's no dedicated UI for this yet; that would be a real future enhancement, not built in this MVP.
7. **When to expand**: once you've run enough real cycles across your pilot clients (rough guideline: 10-20) without a concerning finding, you can reasonably extend it to more clients. There's no code gate stopping you either way — this is entirely your judgment call.

**This closes the full HC-001 through HC-025 execution order.** Every step is either built, applied, and live-verified, or explicitly and deliberately deferred with the reasoning documented in place. The core coaching loop (check-in → task → context → rules → safety gate → AI reasoning → validation → authority → recommendation → coach approval → outcome tracking) works end to end against real production data.
